#!/bin/bash
# Runs commands for cleaning and updating the system.

# Cleaning
sudo cacheclean 1
profile-cleaner gc

# Updating
sudo pacman-color -Syu
packer -Syu --noedit --auronly
gem update
sudo npm update -g
